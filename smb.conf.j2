[global]
   workgroup = WORKGROUP
   server string = Samba Server
   log file = /var/log/samba/log.%m
   max log size = 50
   security = user
   passdb backend = tdbsam

[shared]
   path = /mnt/shared
   writable = yes
   read only = no
   browseable = yes
   writable = yes